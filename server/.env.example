PORT=5000
MONGODB_URI=mongodb://127.0.0.1:27017/roamify
JWT_SECRET=change-me-to-a-long-random-string
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini
# Optional: production frontend URL(s) for CORS (comma-separated). Trailing slashes are stripped.
# CORS_ORIGINS=https://your-app.vercel.app
# Vercel preview URLs differ from production; set true to allow any https://*.vercel.app origin:
# CORS_ALLOW_VERCEL_SUBDOMAINS=true

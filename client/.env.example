# Optional: full API origin when not using Vite dev proxy (e.g. production build).
# Leave unset for local dev — requests go to same origin and Vite proxies /api.
# VITE_API_URL=http://localhost:5000
